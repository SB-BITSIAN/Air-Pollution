"# Air-Pollution"  

The program makes use of libraries numpy, pandas, matplotlib.
This project makes use of the  mini-batch gradient descent approach to solve the problem, hence some noisy loss function is expected to appear (smooth loss function for gradient descent approach).
This project is totally for educational purpose and the primary contributor(s) have no involvement in any of its commercial use.